<div class="pure-g">
    <div class="pure-u-23-24 center-item-h">
        <h3>{{title}} By {{user.name}} on {{format_date date}}</h3>
        <p>{{body}}</p>
        <a href="/viewpost/{{id}}">Comment</a>
    </div>
</div>